apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  mongo-host: mongo-service
  backend-host: backend-service
  mongo-database: address-database?authSource=admin
  backend-port: "3030"
  frontend-port: "3031"